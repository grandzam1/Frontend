# Neon Postgres connection string (server only — never expose to the browser)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require

# Admin unlock PIN for /api/sites mutations
ADMIN_PIN=1234
